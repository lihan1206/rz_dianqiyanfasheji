# 预发布环境配置
flyway.url=jdbc:mysql://${DB_HOST:staging-db}:3306/${DB_NAME:electrical_rnd_staging}?useSSL=false&serverTimezone=Asia/Shanghai
flyway.user=${DB_USER:staging_user}
flyway.password=${DB_PASSWORD:staging_password}
flyway.schemas=${DB_NAME:electrical_rnd_staging}
flyway.locations=filesystem:flyway/sql
flyway.cleanDisabled=true
flyway.validateOnMigrate=true
